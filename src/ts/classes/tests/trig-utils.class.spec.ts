import {} from 'jasmine';

import { TrigUtils } from '../trig-utils.class';

describe('TrigUtils', function() {
	it('should equal fixture contents', function() {
		expect(TrigUtils).toEqual(TrigUtils);
	});

});